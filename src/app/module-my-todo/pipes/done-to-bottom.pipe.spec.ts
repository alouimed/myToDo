import { DoneToBottomPipe } from './done-to-bottom.pipe';

describe('DoneToBottomPipe', () => {
  it('create an instance', () => {
    const pipe = new DoneToBottomPipe();
    expect(pipe).toBeTruthy();
  });
});
